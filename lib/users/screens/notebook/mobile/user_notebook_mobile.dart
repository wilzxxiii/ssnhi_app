import 'package:flutter/material.dart';

class UserNotebookMobile extends StatelessWidget {
  const UserNotebookMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
