import 'package:flutter/material.dart';

class UserNotebookWeb extends StatelessWidget {
  const UserNotebookWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
